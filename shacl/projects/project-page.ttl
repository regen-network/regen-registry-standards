@prefix schema: <http://schema.org/> .
@prefix sh: <http://www.w3.org/ns/shacl#> .
@prefix xsd: <http://www.w3.org/2001/XMLSchema#> .
@prefix regen: <http://regen.network/> .

# Unanchored - single schema for all credit classes

regen:ProjectPageShape a sh:NodeShape ;
  sh:targetClass regen:Project-Page ;

  sh:property [
    sh:path regen:creditClassId ;
    sh:datatype xsd:string ;
    sh:minCount 1 ;
    sh:maxCount 1 ;
  ] ;

  sh:property regen:DescriptionPropertyShape ;
  sh:property regen:PreviewPhotoPropertyShape ;
  sh:property regen:GalleryPhotosPropertyShape ;
  sh:property regen:VideoURLPropertyShape ;

  sh:property [
    sh:path schema:creditText ;
    sh:datatype xsd:string ;
    sh:maxCount 1 ;
  ] ;

  # the following constraint specifies that one of the shapes listed must be satisfied.
  # it means that either VCS/CFC project id and project page must both be present.
  # i.e. it prevents a cfcProjectId being used with vcsProjectPage.
  # if neither of these shapes in the data graph, validation will fail.
  sh:xone (
    [
      sh:property regen:VCSProjectIdPropertyShape ;
      sh:property regen:VCSProjectPagePropertyShape ;
      sh:property regen:VCSMethodologyPropertyShape
    ]
    [
      sh:property regen:CFCProjectIdPropertyShape ;
      sh:property regen:CFCProjectPagePropertyShape
    ]
  )
.
